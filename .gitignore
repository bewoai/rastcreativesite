# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# AI agent tool directories (local only — not part of the project source)
.claude/
.agents/
.codex/
.gemini/

# CI/CD platform config files (Cloudflare Pages etc — not used)
.pages.yml

# source logo files (originals + Illustrator) — optimised versions are in src/assets/logos/
RastLogo/
# client brand logo sources (originals + zips) — used copies live in src/assets/logos/
calistigimiz_markalarin_logolari/

# temporary notes & scratch files
faz4not.txt
projelinkler.txt

# session/deployment reports (stored locally, not part of repo)
CODEX-MEMORY.md
DEPLOY-RAPORU.md
OTURUM-RAPORU-*.md
RAPOR.md
RAST-CREATIVE-YOL-HARITASI.md
YAYIN-DEPLOY-DURUM-RAPORU-*.md
