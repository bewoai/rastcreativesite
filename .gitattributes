# Normalize text line endings to LF in the repo (CI builds on Linux).
* text=auto eol=lf

# Binary assets — never normalize or diff.
*.woff2 binary
*.woff  binary
*.png   binary
*.ico   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.avif  binary
